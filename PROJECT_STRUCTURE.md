# 项目结构说明

## 📁 目录结构

```
webapp/
├── frontend/                    # 前端文件夹
│   ├── index.html              # 主页面
│   ├── README.md               # 前端文档
│   ├── css/
│   │   └── style.css           # 样式文件
│   ├── js/
│   │   ├── config.js           # 配置文件（底图、API、样式等）
│   │   ├── map.js              # 地图管理类
│   │   ├── layers.js           # 图层管理类
│   │   └── main.js             # 主程序入口
│   └── data/                   # 静态数据文件（可选）
│       ├── slums.geojson       # 贫民窟数据
│       ├── buildings.geojson   # 建筑物数据
│       └── ...
│
├── backend/                     # 后端文件夹（可选）
│   ├── app.py                  # Flask 应用
│   ├── requirements.txt        # Python 依赖
│   └── templates/
│       └── redirect.html
│
├── .gitignore                   # Git 忽略文件
├── README.md                    # 项目主文档
└── start.sh                     # 启动脚本（Linux/Mac）
```

## 🎯 核心文件说明

### 前端核心文件

#### `frontend/index.html`
- 主HTML页面
- 引入 Leaflet.js CDN
- 包含地图容器、图层控制面板、信息面板
- 响应式布局

#### `frontend/css/style.css`
- 全局样式
- 控制面板样式
- 信息面板样式
- 弹出窗口样式
- 响应式设计
- 加载动画

#### `frontend/js/config.js`
配置文件，包含：
- API 配置（本地/远程）
- 地图初始中心点和缩放
- 底图图层配置（4种底图）
- 数据图层样式配置

#### `frontend/js/map.js`
地图管理类 `MapManager`：
- 初始化地图
- 创建底图图层
- 切换底图
- 地图导航（飞到、适应边界）

#### `frontend/js/layers.js`
图层管理类 `LayerManager`：
- 初始化图层组
- 加载数据图层（API或静态文件）
- 生成模拟数据（用于演示）
- 样式管理
- 弹出窗口绑定
- 信息面板更新
- 图层显示/隐藏切换

#### `frontend/js/main.js`
主程序入口：
- DOM 加载完成后初始化
- 创建地图和图层管理器实例
- 加载初始数据
- 设置事件监听器（底图切换、图层切换）

### 后端文件（可选）

#### `backend/app.py`
Flask API 服务器：
- 提供 RESTful API
- 返回 GeoJSON 数据
- CORS 支持

#### `backend/requirements.txt`
Python 依赖：
- Flask
- Flask-CORS
- 其他依赖

## 🔄 数据流

### 纯前端模式（默认）
```
用户浏览器
    ↓
加载 index.html
    ↓
加载 CSS/JS 文件
    ↓
初始化地图（Leaflet）
    ↓
生成模拟数据 或 加载静态 GeoJSON
    ↓
渲染到地图
```

### 前后端分离模式
```
用户浏览器
    ↓
加载前端页面
    ↓
发送 API 请求 → Flask 后端
    ↓
后端返回 GeoJSON
    ↓
前端渲染数据
```

## 📊 支持的数据格式

### GeoJSON 标准格式

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "properties": {
        "name": "区域名称",
        "population": 1000,
        "area": 500,
        "risk_level": "high"
      },
      "geometry": {
        "type": "Polygon",
        "coordinates": [
          [
            [经度, 纬度],
            [经度, 纬度],
            ...
          ]
        ]
      }
    }
  ]
}
```

### 支持的几何类型
- Point（点）
- LineString（线）
- Polygon（多边形）
- MultiPoint
- MultiLineString
- MultiPolygon

## 🎨 自定义配置

### 修改地图中心
编辑 `frontend/js/config.js`：
```javascript
map: {
    center: [纬度, 经度],  // 改为目标城市
    zoom: 12
}
```

### 添加新底图
编辑 `frontend/js/config.js`：
```javascript
basemaps: {
    myBasemap: {
        name: '我的底图',
        url: 'https://example.com/{z}/{x}/{y}.png',
        attribution: '...',
        maxZoom: 18
    }
}
```

### 修改图层样式
编辑 `frontend/js/config.js`：
```javascript
styles: {
    myLayer: {
        color: '#ff0000',
        fillColor: '#ff0000',
        fillOpacity: 0.3,
        weight: 2
    }
}
```

## 🚀 部署选项

### 1. GitHub Pages（推荐）
- 免费
- 自动 HTTPS
- CDN 加速
- 适合纯静态前端

### 2. Netlify
- 免费额度充足
- 自动部署
- 表单处理
- 无服务器函数

### 3. Vercel
- 免费
- 快速部署
- 适合 Next.js 等框架

### 4. 自己的服务器
- 完全控制
- 可运行后端
- 需要维护

## 📝 开发流程

1. **本地开发**
   - 启动本地服务器
   - 实时预览修改

2. **测试**
   - 在不同浏览器测试
   - 测试响应式布局
   - 测试数据加载

3. **提交代码**
   ```bash
   git add .
   git commit -m "描述"
   git push
   ```

4. **部署**
   - GitHub Pages 自动部署
   - 或手动上传到服务器

## 🔧 常用命令

### Git
```bash
# 查看状态
git status

# 添加文件
git add .

# 提交
git commit -m "消息"

# 推送
git push origin main

# 拉取
git pull
```

### 本地服务器
```bash
# Python
python -m http.server 8000

# Node.js (需安装 http-server)
npx http-server -p 8000
```

## 📚 学习资源

- [Leaflet 官方文档](https://leafletjs.com/)
- [GeoJSON 规范](https://geojson.org/)
- [GitHub Pages 文档](https://docs.github.com/pages)
- [Markdown 语法](https://www.markdownguide.org/)

## 🐛 调试技巧

### 浏览器开发者工具
- F12 打开控制台
- 查看 Network 标签（网络请求）
- 查看 Console 标签（错误信息）

### 常见问题
1. 地图空白 → 检查 Leaflet CDN 链接
2. 数据不显示 → 检查 GeoJSON 格式
3. 样式错误 → 检查 CSS 文件路径
4. API 连接失败 → 检查 CORS 配置

## 📄 许可证

MIT License - 可自由使用和修改
